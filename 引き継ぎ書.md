# 金型入替シミュレーター 引き継ぎ書

**プロジェクト**: 日建フレメックス株式会社 EMZ3612M2E タレットパンチプレス 金型入替計画ツール  
**ファイル**: `index.html`（単一ファイル完結）  
**公開URL**: `https://ni1214.github.io/katamold/`  
**リポジトリ**: GitHub Desktop → `C:\Users\frx\Desktop\katamold\index.html`  
**デプロイ方法**: ファイル上書き → GitHub Desktop で Commit → Push → 数分で自動反映

---

## 1. プロジェクト概要

タレットパンチプレスの金型入替計画を立てるためのシミュレーターアプリ。  
HTMLファイル1つで完結し、サーバー不要・インストール不要・完全無料で動作する。

### 技術スタック
- **HTML / CSS / Vanilla JavaScript** のみ（フレームワーク・ライブラリなし）
- **localStorage** による自動保存（ブラウザ内蔵）
- **JSONファイル** による手動保存・共有
- **GitHub Pages** による公開ホスティング

---

## 2. ファイル構成・コード構造

`index.html` は以下のブロックで構成されている（すべて1ファイル内）。

```
index.html
├── <style>          CSS（行: 1〜400付近）
├── <body>           HTML構造（行: 400〜580付近）
│   ├── header       ヘッダー（統計・操作ボタン）
│   ├── .layout      2カラムレイアウト
│   │   ├── .left    候補金型リスト + カスタム追加フォーム
│   │   └── .right   タレット配置図 + 変更一覧テーブル
│   ├── #slotOv      スロット選択モーダル
│   ├── #cmOv        カスタムサイズ入力モーダル
│   └── #svOv        保存・共有モーダル（3タブ）
└── <script>         JavaScript（行: 580〜末尾）
```

### CSSのカラー変数（`:root`）
| 変数 | 用途 |
|---|---|
| `--accent` `#00d4ff` | メインアクセント（青） |
| `--accent2` `#ff6b35` | サブアクセント（オレンジ） |
| `--accent3` `#39ff14` | 緑（継続スロット） |
| `--warn` `#ffcc00` | 警告・未保存 |
| `--danger` `#ff4444` | 危険・削除 |
| `--success` `#00e676` | 成功・保存済み |
| `--bg` `#0d1a2a` | 背景 |
| `--steel-mid` `#243447` | カード背景 |

---

## 3. 主要データ構造

### `slots` 配列（タレットスロット）
```javascript
{
  num: 1,           // スロット番号（ステーション内の連番）
  no: 105,          // 金型番号
  st: 'A',          // ステーション（A/B/C/D/E/G）
  shape: '丸',      // 現在の形状
  size: '8Φ',       // 現在のサイズ
  detail: '―',      // 詳細・条件
  // 入替後に追加されるフィールド
  oShape: '丸',     // 元の形状（入替前）
  oSize: '8Φ',      // 元のサイズ
  nShape: '角',     // 新しい形状
  nSize: '10x10',   // 新しいサイズ
  nDetail: 'CL=0.30',
  nNo: 'NEX3-027',
  prevCid: 'c027',  // 候補リストのID（候補から選んだ場合）
  purpose: '',      // 用途メモ
  note: '',         // 備考
  editor: '山田',   // 最終編集者名
  editorColor: '#00d4ff',
  at: '2026/03/01 14:30:00'  // 変更日時
}
```

### `cands` 配列（候補金型リスト）
```javascript
{
  id: 'c001',         // 固定ID（見積書由来: c001〜c042、カスタム: cu-1000〜）
  shape: '丸',
  size: '2.2Φ',
  detail: 'CL=0.30',
  range: 'A',         // 対応ステーション
  no: 'NEX3-001'      // 品番
}
```

### ステーション定義（`ST` 定数）
```javascript
A: 1.6〜12.7Φ   B: 12.8〜31.7Φ   C: 31.8〜50.8Φ
D: 50.9〜88.9Φ  E: 89〜114.3Φ    G: 12.8〜31.7Φ（インデックス）
```

---

## 4. 重要関数の解説

### レンダリング系
| 関数 | 説明 |
|---|---|
| `renderAll()` | 全体再描画（turret + cands + stats + log） |
| `renderTurret()` | タレット配置図を再描画 |
| `renderLog()` | 変更一覧テーブルを再描画 |
| `renderCands()` | 候補リストを再描画（フィルター・検索反映） |
| `slotStat(s)` | スロットの状態を返す `'keep'/'replace'/'new-a'/'empty'` |

### 編集操作系
| 関数 | 説明 |
|---|---|
| `applyC(idx, cid, note, purpose)` | 候補リストからスロットに金型を適用 |
| `applyFree(idx, sh, sz, dt, note, purpose)` | 任意サイズでスロットに適用 |
| `doRestore(e, idx)` | スロットを元の金型に戻す |
| `resetAll()` | 全変更をリセット |

### モーダル系
| 関数 | 説明 |
|---|---|
| `openSlotOv(e, idx)` | スロットクリック時の選択モーダルを開く |
| `openCandModal(cid)` | 候補をクリックした時のスロット選択モーダル |
| `openCmOv(idx)` | 任意サイズ入力モーダルを開く |
| `closeOv(id)` | モーダルを閉じる |

### 保存系
| 関数 | 説明 |
|---|---|
| `buildPayload()` | 現在の状態をJSON文字列に変換 |
| `applyPayload(d)` | JSONオブジェクトを状態に反映 |
| `expJSON()` | `金型入替計画_保存データ.json` でダウンロード |
| `impJSON(input)` | JSONファイルを読み込んで状態に反映 |
| `saveLocalNow()` | localStorageに自動保存 |
| `restoreAutoSave()` | localStorageから復元 |
| `queueAutoSave()` | 700ms後に自動保存（デバウンス）+ 未保存フラグON |
| `markUnsaved()` | 保存ボタンを警告色に変える |
| `markSaved()` | 保存ボタンを通常色に戻す |

### 編集ロック系（競合防止）
| 関数 | 説明 |
|---|---|
| `startEditing()` | 編集モードを開始・ロックを取得 |
| `endEditing()` | 編集モードを終了・ロックを解放 |
| `acquireEditingLock()` | `/api/lock` にロック取得リクエスト |
| `checkEditing()` | 他の人がロック中か確認（ポーリング） |

---

## 5. 保存・同期の仕組み

```
操作
 ↓
queueAutoSave() → 700ms後
 ↓
saveAutoNow()
 ├─ remoteMode=true  → saveRemoteNow() → POST /api/state（サーバーあり時）
 └─ remoteMode=false → saveLocalNow()  → localStorage（GitHub Pages時）
```

**GitHub Pagesで開いた場合**（現在の運用）:
- `isRemoteCapable()` が `false`（file://プロトコル or /api/stateが存在しない）
- localStorageに自動保存
- JSONファイルで手動保存・共有

**ローカルサーバー（Python）で開いた場合**（将来対応可）:
- `isRemoteCapable()` が `true`
- `/api/state`・`/api/lock` にアクセスして複数人同期

---

## 6. 改修履歴

| 日付 | 内容 |
|---|---|
| 2026/03 | 別AIで初版作成。ログイン機能・編集ロック・自動保存・候補リスト・印刷機能 |
| 2026/03 | Claude(Anthropic)で以下を追加 |
| | ・保存ボタンを固定ファイル名（`金型入替計画_保存データ.json`）に変更 |
| | ・JSONの読み込みボタンを保存モーダルに追加（`impJSON`関数） |
| | ・未保存変更の追跡（`markUnsaved` / `markSaved` / `hasUnsavedChanges`） |
| | ・保存ボタンが未保存時に黄色く変わる視覚フィードバック |
| | ・タブを閉じる・リロード時に未保存警告（`beforeunload`イベント） |
| | ・ヘッダーに「💾 保存」ボタンを常時表示（モーダルを開かずワンクリック保存） |
| | ・保存モーダルに「📂 読込・その他」ボタンを追加 |

---

## 7. TODO / 今後やりたいこと

- [ ] **Googleスプレッドシート連携**: 保存をGoogle Apps Scriptで自動化すれば完全クラウド運用可能
- [ ] **名前入力画面**: 現在は「共有モード」固定。ログイン画面を追加して担当者名を入力できるようにする
- [ ] **変更のコメント機能**: 各スロットの変更に理由・コメントを付けられるようにする
- [ ] **印刷レイアウトの改善**: A4横向きで綺麗に収まるよう調整
- [ ] **スロットの並び替え**: ドラッグで並び順を変更できるようにする
- [ ] **見積書との金額連動**: NEX3シリーズの単価を持たせて合計金額を自動計算

---

## 8. デプロイ手順

```
1. index.html を修正・保存
2. GitHub Desktop を開く
   → 左側に変更ファイルが表示される
3. 左下「Summary」に変更内容を入力（例: "保存機能を改善"）
4. 「Commit to main」をクリック
5. 上部「Push origin」をクリック
6. 2〜3分後に https://ni1214.github.io/katamold/ に反映
   ※ Chromeで Ctrl+Shift+R（強制リロード）で確認
```

---

## 9. ローカル動作確認

HTMLファイルをブラウザで直接開くだけで動作確認できる。

```
C:\Users\frx\Desktop\katamold\index.html
↓ ダブルクリック or ブラウザにドラッグ
Chrome / Edge で開く → そのまま動作
```

`/api/state`・`/api/lock` のAPIは呼び出されるがエラーになっても問題なし（localStorageモードに自動フォールバック）。

---

## 10. AIへの引き継ぎプロンプト（Cursor / Copilot用）

以下をIDEのAIチャットに貼り付けて使う:

```
このindex.htmlは日建フレメックスの金型入替シミュレーターです。
HTML/CSS/Vanilla JSの単一ファイル完結アプリです。

【構造】
- slots配列: タレットスロット59件（A/B/C/D/E/Gステーション）
- cands配列: 候補金型リスト（見積書由来42件 + ユーザー追加分）
- localStorageで自動保存、JSONファイルで手動保存・共有
- GitHub Pages (https://ni1214.github.io/katamold/) で公開

【保存の仕組み】
- queueAutoSave() → saveLocalNow() → localStorage
- expJSON() → 固定ファイル名でダウンロード
- impJSON() → JSONファイル読み込み

【編集時の注意】
- renderAll()を呼ぶと全体再描画される
- 状態変更後は必ずqueueAutoSave()を呼ぶ（未保存フラグが立つ）
- slotStat(s)でスロットの状態（keep/replace/new-a/empty）を取得

以下の修正をお願いします：
[ここに修正内容を書く]
```
